# Browser address; replace YOUR_SERVER_IP and use the actual published port.
ANYROUTER_ORIGIN=http://YOUR_SERVER_IP:8787
# Login user: admin. Set a long random password. Single quotes preserve $ characters.
ANYROUTER_PASSWORD=''
# Compose host port. The container listens on 8787.
ANYROUTER_HTTP_PORT=8787
