# Codex 多任务探活 / 保活 3.1.0

一个终端运行多个独立任务，支持鼠标点击、键盘操作、随机保活和任务组保存。需要 **Python 3.11+**，只用 Python 标准库，支持 Windows、Linux 和 macOS。

Windows 和 Linux 已进行终端实测；macOS 使用同一套 POSIX 输入接口，目前没有 macOS 实机测试环境。鼠标需要终端支持上报；不支持时仍可使用键盘。

## 启动和升级

- Windows：完整解压后双击 **启动脚本.bat**。
- Linux/macOS：在解压目录执行 **sh start.sh**；macOS 也提供 start.command。
- 需要同时更新压缩包里的 **14 个文件**。多任务与通知模块包括 codex_tasks.py、codex_board.py、codex_mouse.py、codex_notify.py，不能只替换主脚本。
- 保留原数据目录。首次使用工作台时，旧的单 API 默认配置可以作为一个任务载入；原 API、密钥和历史记录继续保留。
- 已经打开的旧进程仍运行旧代码，重新启动后确认版本为 **3.1.0**。

正常交互终端默认打开多任务工作台。可以显式选择运行方式：

```sh
# 多任务工作台
python codex_poll.py --multi

# 保留原来的单任务界面和命令行行为
python codex_poll.py --single

# 关闭鼠标，继续使用键盘
python codex_poll.py --multi --no-mouse
```

## 启动前仍需确认

有已保存任务组时，交互启动先问 **是否沿用已保存配置**，不会自动替你确认。

- **沿用**：恢复任务组、各任务的连接、模型、时间和启用状态，确认后才发送请求。
- **不沿用**：打开空工作台，重新添加任务；已有 API 和历史记录保留。未保存更改便退出，原保存任务组仍保留。
- **退出**：不发送请求。

显式传入连接参数或 --task 属于明确选择，直接使用指定连接。后台/重定向运行沿用保存的任务组。--setup 重新选择；--settings 只修改默认模板；--show-config 只看配置。

## 主界面

顶部显示全部任务、保活、探活、暂停和正在请求的数量。任务列表显示名称、连接/模型、运行状态、最近 HTTP、下次发送时间；选中任务后，宽窗口右侧显示时间、会话 ID 和操作。

宽终端使用“任务表 + 右侧详情”；窄终端收起右侧面板，可进入独立详情页。任务多时滚动列表。完整界面建议至少 64 列、20 行，较大窗口能显示更多信息。

总览下方的 **最近变化** 会利用任务列表到底部操作栏之间的剩余高度，一行显示一条，窗口放大后自动增加显示数量。保留本次运行最近 200 条变化；空间不足时收起，完整请求记录仍可通过 **记录** 页面查看。

终端使用等宽文字、边框和颜色表达布局。圆角、阴影和比例字体与 HTML 设计稿不同，主要信息结构与操作保持一致。

## 点击和快捷键

| 操作 | 鼠标 | 键盘 |
|---|---|---|
| 选择任务 | 点击任务行 | ↑ / ↓ |
| 打开详情 | 双击任务行或点击详情 | Enter |
| 选择按钮 | 直接点击 | Tab，随后 Enter |
| 暂停 / 继续当前任务 | 点击任务行或底部按钮 | P / R |
| 单次探活 | 点击单次探活并确认 | V |
| 修改时间 | 点击时间按钮 | T |
| 自定义输出 token 上限 | 点击右侧输出上限或详情页 Token 按钮 | B |
| 修改任务参数 / 切换连接 | 点击任务设置 | S |
| 查看当前任务记录 | 点击记录 | H |
| 新增任务 | 点击顶部新增 | N |
| API 管理 / 默认设置 | 点击顶部页签 | M / G |
| 搜索任务或记录 | 点击查找 | / |
| 翻页 | 滚轮 | PgUp / PgDn |
| 返回总览 / 取消 | 点击返回 | Esc |
| 退出整个工作台 | 点击退出，再确认范围 | Q |

任务级快捷键在总览和任务详情使用。记录、API、默认设置页面显示各自的操作范围，避免误操作隐藏的任务。极小窗口可按 Enter 打开操作菜单。

点击字段或按 Enter 后进入文字输入，输入完成按 Enter；空输入保留原值。选择字段会显示选项菜单。密钥输入不回显。Esc 或取消放弃整份草稿；确认保存后才应用。

鼠标在松开按钮时生效；拖动和 Shift 选择不会触发任务操作。需要复制终端文字时，可尝试按住 Shift 拖动，或使用会话/记录的复制按钮。Linux/SSH 的复制按钮使用 OSC 52，需要客户端支持。

## 添加、编辑和移除任务

点击 **新增任务**，可选择已保存 API、输入新 API，或读取当前 Codex 配置。确认任务名称、模型、探活间隔、保活范围、超时和输出上限，最后选择：

- **待启动**：保存任务后保持暂停，点击继续时才开始。
- **立即启动本任务**：保存后开始该任务的请求。

选择已保存 API 时会载入它的已保存设置；新连接可使用默认模板。任务建立后拥有自己的参数，之后修改模板不会覆盖已有任务。

**任务设置 → 名称、模型、时间与请求设置**：修改该任务。**切换 API / Codex 连接**：为此任务重新选择连接，并保留它自己的时间、请求和通知参数。

任务创建时保存独立配置，包括连接、模型、密钥、时间、输出上限、工具模式和 Telegram 通知。修改默认模板、API 库或 Codex 配置后，已有任务在运行中和重启后都保持原配置；新建任务才读取更新后的配置。要更新旧任务，请进入该任务单独修改。旧版本任务在首次确认沿用并启动时保存快照，凭据按当前平台的保护方式独立存储。

编辑一个任务、浏览记录或等待模型列表时，其他任务继续运行。正在发送的请求保留发送时的配置快照；旧响应不会覆盖新连接状态，也不会替新连接更换会话。

## 自定义输出 token 上限

选中任务后按 **B**，或点击右侧 **输出上限** 的数字，进入独立的设置页。点击字段或按 Enter，输入想要的正整数，例如 **64、128、300、512**，再选择 **保存修改**。也可以从 **任务设置 → 输出 token 上限** 进入。数值可以自行输入，具体可用范围由模型接口决定。

修改只应用到当前任务的后续请求，已发出的请求沿用发送时的上限；开启记忆时会保存到任务组，下次启动选择沿用即可恢复。已有保存值（例如 300）不会被升级覆盖。

按 **G** 打开默认设置，再按 **B**，可以修改新连接使用的默认输出上限；也可在 **请求与并发** 页点击输出上限。从已保存 API 创建任务时保留该 API 的设置，可在新增任务表单里修改；已有任务分别设置。

这是每次模型响应的输出上限，上限不是每次固定消耗量。输入问题、工具定义等输入 token 另计；含推理的模型可能把推理 token 也计入输出预算。一次逻辑题若触发工具续答，每次模型响应分别受此上限约束，记录中的总用量会合并计算。

如果此前使用 `--token-param none` 关闭了上限参数，从 **B** 设置页保存后会重新启用自动选择的上限字段。命令行也可指定，例如 `python codex_poll.py --multi --max-tokens 96`。

暂停停止该任务后续发送，已发请求可以继续返回。单次探活先暂停持续发送，完成一次后仍暂停；如果已有请求占用该任务的并发位置，会在位置释放后执行。移除任务停止其后续发送，API 与历史记录保留。

顶部的 **暂停全部 / 继续全部** 明确操作整个终端，并先确认范围。退出同样确认所有任务范围。关闭进程不能保证上游已收到的请求被取消。

## 独立调度、会话和并发

- 每个任务分别计时，分别保存状态、配置和本次会话。同一个 API 建立两个任务，也使用两个独立会话。
- 首次立即探活。模型请求返回 **HTTP 200** 后自动进入保活，默认每轮随机 **60–90 秒**；非 200 或本地超时恢复固定探活，默认 **2 秒**。
- 每个任务可设置自己的最短/最长保活间隔。两者相等就是固定间隔。计时以实际发送时刻为基准。
- 400 是否更新会话 ID 按任务配置处理，只影响该任务。暂停/继续保持会话；任务内切换连接后切回，可复用该任务本次运行中对应连接的会话。
- 慢任务不阻塞其他任务。全局默认最多 **8** 个同时请求，每个任务默认最多 **1** 个，均可配置为 1–64。
- 达到上限显示 **等待空位**，不会积累补发队列。超时或移除后，底层尚未返回的网络请求仍占用其物理位置，直到返回或网络超时，以免不断堆积线程。
- 每个任务的结果按自身请求编号保存，另一个任务不会被它的慢响应挡住。保存的记录还包括 task_id、task_name、task_label 和本次 run_id。

## 状态与完整错误

HTTP 200 为绿色；4xx 为黄色；5xx、网络错误和超时为红色。运行状态与最近 HTTP 分开展示：暂停的任务仍可以看到它上次的 HTTP 结果。

列表中用简短提示显示问题，例如 **输出达上限**。点击记录或用方向键和 Enter 打开，可查看完整问题、回答、原始错误、会话 ID 和工具调用。

**incomplete / max_output_tokens** 表示输出预算耗尽。HTTP 200 仍按你的状态码规则进入保活，但回答可能未完成。输出上限包含内部推理；可以在任务设置中将 128 改为 256 或 512。上限不是每次固定消耗量。

## 记录管理

请求记录可按任务、HTTP 200/非 200/输出未完成和关键词筛选。列表显示范围内最近 1000 条，可点击刷新；旧版未标记任务 ID 的记录在 **全部任务** 查看。

- 打开一条记录，点击 **删除此条 D** 可删除单条。
- 点击 **删除** 可删除当前筛选出来的记录，或明确选择清空当前任务/全部记录。
- 删除前显示范围，默认选中取消。删除后清理对应列表缓存，任务和 API 保留。
- 本次运行完成次数不因删历史而回退；运行中的任务仍可能产生新记录。
- --log-file 单独追加的 JSONL 文件不随数据库删除操作改写。

## API 管理与默认模板

API 管理显示连接、默认模型和被多少任务使用。新增/编辑连接时验证模型列表；密钥以隐藏输入方式处理。更换地址来源时需要重新填写该平台密钥，避免把旧密钥误用于其他站点。

编辑 API 库只影响之后使用它新建的任务；现有任务继续使用自己的连接快照。正在被任务组使用的 API，需要先切换或移除相关任务才能删除。

默认设置按 **启动、时间模板、请求与并发、通知、显示、记录** 分组。启动确认一直保留；只能选择确认配置后开始已启用任务，或先暂停。任务模板用于新任务，已有任务独立修改；总并发和显示属于本终端的运行控制。

## Telegram 探活成功通知

多任务工作台支持 Telegram Bot。普通通知使用标准 Bot API，不启用付费广播，也不需要额外服务器或域名。通知默认关闭，填写配置后自行开启。

1. 在 Telegram 打开 **@BotFather**，发送 `/newbot` 创建机器人，复制 **Bot Token**。
2. 程序中按 **G → 通知 → 配置 Telegram → 编辑 Telegram 配置**，先保存 Token，保持通知关闭。
3. 给新机器人发送 `/start`，返回程序选择 **读取 Chat ID**，确认自己的聊天或目标群组。已有 webhook 的机器人可能不能读取 updates，可使用专用机器人或手动填写 Chat ID。
4. 编辑配置开启通知并保存，返回通知设置页点击 **测试连通**，或在配置菜单中点击 **发送测试通知**。程序会真实发送一条测试消息，结果显示在最近变化及通知设置中；手机收到消息即可确认完整通知链路。
5. 以上设置供新任务使用。已有任务按 **S → Telegram 通知**，可以单独填写，也可以选择 **将默认通知配置应用到此任务**。修改默认通知不会更改这些旧任务。

通知包含任务编号/名称、模型、HTTP 状态、响应耗时、保活范围和时间，默认不推送 API 密钥、完整接口地址或问答内容。HTTP 200 触发首次成功通知；之后只有失败再恢复成功才通知，持续保活成功不重复发送。`incomplete` 的 HTTP 200 仍视为成功，并附输出未完成提示。首次成功和恢复通知分别可关闭。

每个任务默认冷却 **300 秒**，可以修改。短时间内同一接收位置的多个成功通知会合并，按每条消息最多 3800 UTF-16 单位分批，同一接收位置发送间隔至少 3 秒。网络错误、服务错误最多额外重试两次；遇到限流按 Telegram 返回的等待时间重试。通知发送使用独立队列，失败不影响探活或保活状态。

指定 `--count` 时，请求次数完成后会提前提交待发送通知，最多等待 12 秒，再结束进程；用户主动退出时会取消排队中的通知。

运行电脑需要能访问 **api.telegram.org**，可在通知配置中填写 HTTP(S) 代理，例如 `http://127.0.0.1:7890`；留空时使用系统/环境代理。Bot Token 和带凭据的代理地址不回显，日志脱敏。Windows 使用 DPAPI；Linux/macOS 使用权限受限的本地数据库。**已提交 Telegram** 表示接口接受，不代表手机已读。

通知功能在多任务工作台使用，原 `--single` 模式保留兼容行为。

## 200 道逻辑题与工具请求

内置 200 道不重复的短逻辑题，按各任务编号轮换。没有问候或“回复 OK”题目；每次只发送一道题，默认简短回答。参考答案只保存在 logic_questions.json 和 200道短逻辑问答.md，不发送给模型。

默认 client 格式参考客户端结构：Responses 的 additional_tools、functions 命名空间、工具定义、开发者声明和当前问题。继续携带 prompt_cache_key 与 reasoning.encrypted_content 等兼容字段。

| 工具模式 | 用途 |
|---|---|
| client | Responses 原生命名空间；Chat 按其协议使用标准函数工具 |
| compatible | 通用函数名称，适配较旧的兼容接口 |
| off | 只发短问题，不发送工具定义 |

已实现的本地工具是 functions.calculate 和 functions.count_occurrences，只做受限数值计算与文本计数，不访问文件或网络。每道题最多处理一轮工具调用、最多两个模型 HTTP 请求；共用该任务的会话与总超时，用量合并为一条逻辑记录。下一题不累加前题对话。

## 命令行与后台运行

```sh
# 查看保存的 API，随后用名称或 ID 创建多个任务
python codex_poll.py --list-saved
python codex_poll.py --multi --task "主线路" --task "备用线路"

# 同一 API 可重复使用；每个任务的会话独立
python codex_poll.py --multi --task "主线路" --task "主线路"

# 覆盖全局/单任务并发和每次响应上限
python codex_poll.py --multi --concurrency 8 --task-inflight 1 --max-tokens 256

# 多任务模式下 count 表示每个启用任务的请求次数
python codex_poll.py --multi --task "主线路" --task "备用线路" --count 3 --no-controls

# 沿用任务组后台运行（先完成一次交互配置）
nohup sh start.sh --multi --no-controls --no-start-paused --display compact > keepalive.log 2>&1 &

# 单独打开默认模板设置，或只查看配置
python codex_poll.py --multi --settings
python codex_poll.py --multi --show-config

# 重新选择任务；或回到原单任务方式
python codex_poll.py --multi --setup
python codex_poll.py --single --saved-api "主线路"
```

连接、模型、时间、格式和记录参数继续兼容：

| 参数 | 默认 / 说明 |
|---|---|
| --multi / --single | 多任务 / 原单任务；支持的交互终端默认多任务 |
| --task | 已保存 API 名称或 ID，可重复指定 |
| --concurrency | 多任务总同时请求上限，默认 8 |
| --task-inflight | 覆盖每个任务的同时请求上限，默认 1 |
| --mouse / --no-mouse | 启用/关闭鼠标点击，默认尝试启用 |
| --interval / --probe-interval | 探活秒数，默认 2 |
| --keepalive-min / --keepalive-max | 保活随机范围，默认 60–90 秒 |
| --success-interval | 显式使用固定保活间隔，不能同时传随机范围 |
| --timeout / --request-timeout | 单个逻辑请求总时限，默认 30 秒 |
| --max-tokens | 每次模型响应的输出上限，默认 128，任务可单独设置 |
| --tool-mode | client / compatible / off |
| --token-param | auto / max_tokens / max_completion_tokens / max_output_tokens / none |
| --count | 单任务模式的总数；多任务模式每个启用任务的次数，0 为无限 |
| --mode | api / codex |
| --base-url / --api-key / --model | 明确指定连接；手动 API 验证模型列表 |
| --saved-api | 单一保存的连接；多项使用 --task |
| --codex-config / --profile | 指定 Codex 配置或 profile |
| --api-style | auto / responses / chat |
| --stream / --no-stream | Responses 默认流式，可显式调整 |
| --prompt | 可重复传入本次运行的自定义问题 |
| --log-file | 追加 UTF-8 JSONL，包括任务标识和工具用量 |
| --memory / --no-memory | 默认保存 API、任务组和记录；关闭时仅保留本次 |
| --data-dir | 指定独立数据目录 |
| --display | auto / dashboard / compact / verbose |
| --color | auto / always / never，支持 NO_COLOR |
| --start-paused / --no-start-paused | 确认配置后先暂停 / 开始 |
| --reset-session-on-400 / --no-reset-session-on-400 | 覆盖任务的 400 会话处理 |
| --max-inflight | 原单任务模式的并发/缓冲上限，默认 64 |

显式命令行参数优先。多任务模式只继承公共时间/请求覆盖，已保存 API 的凭据始终与自己的连接成对读取，不从别的任务借用密钥。

## 数据目录和跨平台

- Windows：默认脚本旁的 codex_poll_data，凭据使用当前 Windows 账户的 DPAPI 加密。
- Linux：默认 $XDG_DATA_HOME/codex-keepalive，未设置时用 ~/.local/share/codex-keepalive。
- macOS：默认 ~/Library/Application Support/CodexKeepalive。
- 各平台优先沿用已存在的脚本旁 codex_poll_data，也可用 --data-dir 明确指定。
- Linux/macOS 凭据以明文存入权限 600 的数据库，专用目录权限 700；公开任务组配置不含密钥，独立任务凭据和通知凭据保存在同一受保护数据库的私有配置表中。
- 从 Windows 复制数据库到其他系统后，需要重新填写原 DPAPI 密钥；原 API 和历史记录可保留。旧 Windows 配置路径也需调整。

数据库增加任务标识列和任务组设置，升级自动迁移。已有历史、API 和单任务默认配置不删除。不同终端各自运行快照，保存新的默认任务组不会重定向已经运行的其他窗口。

macOS 的 start.command 与 Linux 的 start.sh 使用同一份 Python 代码，启动脚本能找到常见 Python 3.11+ 和 Homebrew 路径。退出正常恢复鼠标、回显和光标；Ctrl+C / SIGTERM 可结束运行。

完整帮助：python codex_poll.py --help。
